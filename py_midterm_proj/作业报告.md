**3000以内的数据大小，比如说用户的标注等等，往往没有向量化；对大型数据框进行的操作都是向量化。**

**我没有使用user子目录，每个task都单独存储数据、单独组织代码，在文件夹包含zip中数据情况下，一次run_all可运行到底。**

# Task 1

##### **需要完成**：
综合观影信息、评分信息，设计合理方案分别筛选出前20部比较流行的（rating > 300）男性/
女性偏好电影。
 针对不同类型的电影（genres），统计分析男/女偏好程度（需要做归一化），通过双色直方图
对比显示。

#### 步骤：

* 准备数据：

提取所有user的性别信息-users表格；

提取所有电影的类别和标题-movies表格；

提取所有评分信息-ratings表格；

去掉不流行的；

上面的按照user_id 为left_on和right_on合并，得到一个最全的表格task1_df

<img src="C:\Users\wangr\AppData\Roaming\Typora\typora-user-images\image-20230504004523022.png" alt="image-20230504004523022" style="zoom: 67%;" />

* #### 绘制对比直方图：

 **分别以电影类型和性别作为索引，统计每一类下的总观看数和评分数据;**

```python
df_to_comp=task1_df.groupby(['movie_id','gender']).agg({'movie_id':'count','rating':'mean'})
df_to_comp.columns=['watched','ratings']
```

下图说明：男性和女性都爱看喜剧和剧情片，男性喜欢动作片的程度比女性高很多

![](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\top10.png)

评分数目，男性在动作片，恐怖片，冒险片上的评分明显高，但是注意到女性人数本来就少，实际上一些差不多评分数目的才体现出了女性的偏好；另外女性更爱评价喜剧片和剧情片。

![](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\freq.png)

下图说明，男性上这个电影评分的比例本来就比女性高，因为大多数类型都是男性看的多，其中特殊的是浪漫题材影片，发生了反向超越，这说明这一类别大大地受到女性的喜欢；此外音乐剧也是如此。这两类应该成为分类男性和女性重点关注的对象。

![ifnot](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\ifnot.png)

下面这张图中，蓝色柱子说明男性偏爱，粉色柱子说明女性偏爱。为什么蓝色可以出现较低比例而超过1的比例很少？个人观点是因为女性的评分数量本就是男性的四分之一左右，和男性相比，女性不喜欢上这个网站评分。

![ifnot_colored](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\ifnot_colored.png)

这是每一类别的低分率（就是打了1分的比例）：女性给恐怖惊悚片、西部片打低分的比例要高出不少，而男性明显对音乐剧，儿童剧不感冒。这是可以作为性别分类的标准。

![lows](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\lows.png)

这张图就是平均打分，体现的也很明显，偏好的电影和前面结果大致相同。

![rate](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\rate.png)

* #### 给出前20代表电影：

* 所用的数据框名叫df_to_comp<img src="C:\Users\wangr\AppData\Roaming\Typora\typora-user-images\image-20230504011147411.png" alt="image-20230504011147411" style="zoom: 80%;" />

方案一：

有两个变量，男女观看数之差和男女平均打分之差，这两者的权重难以确定。所以设置动态的权重即ind_score。对八个不同权重方案分别计算并排名，得到的序列进行取交集，对所有权重都排在前面的电影才能入选。

```python
ind_score=np.arange(0.1,0.9,0.1)
def score_2(x,i):
    return ratio*(x.iloc[0,0]-x.iloc[1,0])*ind_score[i]+(x.iloc[0,1]-x.iloc[1,1])*(1-ind_score[i])
    ##########ratio是男女比，正规化
############################################
    result=[]
for i in range(8):
    res=df_to_comp.groupby(level=0).apply(lambda x:score_2(x,i)).sort_values(ascending=False)
    result.append(res)
fav_female=[]
for i in result[0].index:
    flag=1
    for res in result:
        if res.index.get_loc(i)>=40:##############分数高的推荐给女性
            flag=0
    if flag==1 and i not in fav_female:
        fav_female.append(i)
```

方案二：

```python
def score_1(x):
    return x.iloc[0,0]/w2*x.iloc[1,1]-x.iloc[1,0]/w1*x.iloc[0,1]
######################其他排序如上类似
```

得到的总结果在程序中；

方案一的前五名是

* 男：回到未来 虎胆龙威 星球大战前传：幽灵的威胁 土拨鼠之日 2001太空漫游
* 女：辣身舞 钢木兰 安娜斯塔西娅 法式热吻 紫色

方案二的前五名是

* 男：超人 黑衣人 虎胆龙威2  黑暗城市 星河战队
* 女：莎翁情史 理智与情感 美国丽人 小猪宝贝 巧克力情人

**人工标注纯粹基于人对电影的感受，不带有任何数据的角度**

人工标注（男生看男性的，女生看女性的）结果：

女同学认为应该选方案一占八成，选取几个代表性的理由：方案一的女性形象塑造整体更好；方案二的情感状况复杂，狗血/男性歧视/性别偏见化的情节更多；方案一里《罗马假日》排名更高；思想性、时代性更强，等等。

男同学的看法未能达成一致，理由包括：左边的经典电影更多、不看星战、不看星际迷航、不看异形、喜欢教父，等等。

<font color="#dd00dd">一些思考：从人工标注的结果，和上面的图表中可以看出，女性对待评分的认真程度高于男性。女性更倾向于对剧情片这样的片子更多地评价，而且对电影的看法更明确。很有可能，男性的打分更加取决于一时冲动和当时的直觉，打分与电影真实应得的分数相关性可能更低。</font>

# Task 2

**需要完成：**
**´ 拆分训练集-测试集（20%评测），实现评测方案（准确率-召回率）**
**´ 实现分类器模型，对观影数超过100的用户进行预测。调整模型及参数。包括并不限于特征降维**
**来获得较好的效果。**

略去前面提取和合并的步骤，使用的汇总数据如图

<img src="C:\Users\wangr\AppData\Roaming\Typora\typora-user-images\image-20230504015720974.png" alt="image-20230504015720974" style="zoom:67%;" />

* #### 性别预测

从上一个task中获知，western,action,musical的评分数量和平均分在男性女性之间有较大差异。

下面的数据框，选取了后四列作为训练特征。

<img src="C:\Users\wangr\AppData\Roaming\Typora\typora-user-images\image-20230504021403204.png" alt="image-20230504021403204" style="zoom:80%;" />

使用的分类器是xgboost，网格搜索寻找参数搭配。

```python
param_grid = {
    'learning_rate': [0.01, 0.05,0.1],
    'max_depth': [3, 5],
    'min_child_weight': [1, 3,5],
    'subsample': [0.6, 0.8],
    'colsample_bytree': [0.6, 0.8],
    'n_estimators': [100]
}

xgb_model = XGBClassifier()
grid_search = GridSearchCV(xgb_model, param_grid, cv=5, scoring='f1_macro')
grid_search.fit(X_train, y_train)
best_model = grid_search.best_estimator_
y_pred = best_model.predict(X_test)
```

```
         precision    		recall    f1-score   

           0       0.53      0.22      0.31       
           1       0.79      0.94      0.86      

    accuracy                           0.77      
   macro avg       0.66      0.58      0.59       
weighted avg       0.73      0.77      0.73      
```

分类结果如上，男性的预测结果明显较好，这可能是因为女性用户比较少的缘故。

<font color="#dd00dd">一些改进的想法：</font>

 <font color="#dd00dd">可以选取更加针对女性的评分设计的特征，比如对romance的观看比例高低；action这个特征的选取是不是不太恰当，因为男女对action的评分数目差距不够明显；可以尝试离散化其中的计数；同时观看了很多类代表性电影的人数太少了，补零会破坏分类器收敛性，如果要从对不同类别的评分下手有困难，训练集太小——是否可以换一种思路，把几十万的单个评分数据单拉出来训练？能用上更先进的分类方式？</font>

* #### 年龄预测

选取用户观看影片的平均上映时间、儿童片，剧情片，冒险片的偏好来构建特征。下面的图表明这几个特征关于年龄的单调性是较好的，与年龄存在着相关性。

![](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\grade_1.png)

![grade_2](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\grade_2.png)

<img src="C:\Users\wangr\AppData\Roaming\Typora\typora-user-images\image-20230504023017924.png" alt="image-20230504023017924" style="zoom:50%;" />

这是离散标签化的训练集，使用随机森林分类，准确率为42.2%。召回率为18.4%。

<font color="#dd00dd">！！！从task3发现的重要结论：年龄分段的表现之所以不好，很有可能是大人要带娃，导致不少年龄35岁，有正经职业的人评价了好多儿童片。。。</font>怎么解决这个问题呢



# Task 3

**在movielens 1M的数据集上，通过观影-评分及电影简介等信息，对观影>100的用户实现用户画像。**

* #### 用户画像

* **实现了基于SVD的电影协同过滤，计算出了各个电影之间的相似度：**

```python
U, sigma, Vt = svds(matrix_t, k = 20)
sigma = np.diag(sigma)
all_movie_predicted_ratings = np.dot(np.dot(U, sigma), Vt)
all_movie_predicted_ratings_mean = np.mean(all_movie_predicted_ratings, axis=0)
movie_similarity = 1 - pairwise_distances(all_movie_predicted_ratings_normalized, metric='cosine')
```

接下来可以**这样得出每个用户的推荐/表征电影**：

每个用户打5分的电影中，被同龄人观看次数最多的电影取前5名--和这5个电影最相似（依据相似度矩阵），

且这个用户没看过的5个电影。

<font color="#dd00dd">但是这种基于协同过滤的推荐方法也有其弊端：可能表征的电影雷同，都被少数很优质的的电影占据了。</font>

* **找出了每个用户喜欢不喜欢的电影类型：**

```python
df_ex=task3_df.explode('movie_genre')
ex_grouped=df_ex.groupby(['user_id','movie_genre']).agg({'rating':'mean','movie_id':'count'}).reset_index()
ex_grouped['score'] = ex_grouped['rating'] * 10 * ex_grouped['movie_id']
sort = (ex_grouped.groupby(['user_id', 'movie_genre'])
               .apply(lambda x: x.sort_values('score', ascending=False))
               .reset_index(drop=True))
sorted_df = sort.groupby('user_id').apply(lambda x : x.sort_values(by= 'score', ascending=False))
sorted_df = sorted_df[['user_id', 'movie_genre', 'score']]
```

得到如下表格sorted_df，这样查找每个用户最喜欢或者不喜欢的类型就轻而易举了。

<img src="C:\Users\wangr\AppData\Roaming\Typora\typora-user-images\image-20230504044601197.png" alt="image-20230504044601197" style="zoom: 67%;" />

* #### 用户标注

定义‘争议之作’：1分和5分比例都大于10%,比较流行的

定义“怀旧经典”：观看的人当中年龄56以上的比例达到15%，且均分在3.5分以上的

定义“新潮科幻”：类型是Sci-Fi,观看的人当中年龄18,25比例都超过20%，比较流行，且均分在3.5分以上的

定义'浪漫爱情”：类型是romance，观看的人当中女性比例超过50%的

```python
#####例子：怀旧经典的筛选方法#####

ages=pd.DataFrame(df.groupby(['movie_id'])['age'].value_counts())
total_views = ages['age'].sum(level='movie_id')
age_56 = ages.loc[(slice(None), 56), :]
prop_56 = age_56['age'] / total_views

oldpeople=[]
for i in list(prop_56[prop_56 > 0.15].index.get_level_values('movie_id').unique()):
    if (rates[i]>=3.5):
        oldpeople.append(i)
```

由此得到四个列表，分别叫做diverged,oldpeople,scifi,females。对应我们对电影的标注；

对每个用户，统计他们看过这四类电影的频次，可以得到对用户的标注。

```python
prompting=df.iloc[:,0:2]
user_diverged_count = prompting[prompting['movie_id'].isin(diverged)].groupby('user_id').size()
result_div = user_diverged_count[user_diverged_count > 30].index.tolist()
####其余如法炮制
```

标注的输出都是列表形式，包含一系列id。每个用户标签大约有100个用户被包含在内。

从两个取交集的结果可以看出，在100多个标注中，只有5个取到了爱看新潮科幻片和爱看浪漫爱情片的交集，7个取到了爱看怀旧科幻片和爱看浪漫爱情片的交集，7个取到了爱看新潮科幻片和爱看怀旧片的交集。

<font color="#dd00dd">这样的结果是令人满意的。在我们**设置了很高的要求（“提纯”）** 的情况下，总的被标注的用户数量大概占到了用户总数的十分之一；如果增加标签数量，比如说加入职业，加入看其他类型片（比如战争片）的考量，是能够实现对绝大部分用户的有效标注的。</font>

* #### 用户偏好雷达图

找出看电影数目前9的big_fans，计数他们看过各种电影的频次，正规化（除以所有人看过相应电影的频次），绘制雷达图

（在这里，雷达图只注重看倾向就可以，上面的数字没有实用意义）

结合用户信息，下图可以得到丰富的结果。

（1）前两个爱看音乐剧和浪漫片的人都是作家；第三个是经理，喜欢看西部片可以理解。

（2）中间三个人有两个是有正经工作的中青年人，很可能是带孩子的。

（3）后面三个全都是喜欢寻求刺激的中年男人。

（4）既然我们知道了有些中年人会因为带孩子等可能出现的原因而看很多儿童片，那么我们要重新审视年龄预测标签的设定。

（5）有了我们目前拥有的数据，基本上可以做到精准投放。

<font color="#dd00dd">进一步的可以做的：可以单独对女性排序，选取看过电影最多的女性做同样的分析。</font>



![](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\radar1.png)

![radar2](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\radar2.png)

![radar3](D:\Jupyter Workspace\胡俊峰 Python导论作业\大大大大作业\radar3.png)





